import { Placeholder } from '@/components/Placeholder';

export default function Settings() {
  return <Placeholder title="Settings" subtitle="Units, theme, and sign-out land in a later stage." />;
}
